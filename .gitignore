# Secrets & env
.env
secrets/
*.token.json
youtube_token.json

# Generated media & queue
data/pending/*
data/published/*
data/work/*
!data/**/.gitkeep
# control/log files (kept local; never commit)
data/PAUSED
data/auto.log
# NOTE: data/used_*.json, data/last_voice.txt and data/costs.jsonl ARE tracked
# so the cloud job can persist dedup state + the spend ledger between runs.

# Local editor/agent settings
.claude/settings.local.json

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
